<div class="mt-4">
  <%= form_with(model: [post, comment], local: true, remote: true) do |f| %>
    <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに", class: "w-100" %>
    <%= f.submit "送信" %>
  <% end %>
</div>
